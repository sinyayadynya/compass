title: compass/components/block/Search Form Block
parameters:
 sourceLink: 'compass:search_form_block'
stories:
  - name: 1. Default
    args:
      entity_id: compass_search_form_wide
      entity_type: block
      view_mode: default
      langcode: ''
      check_access: true
      search_block_content: |
        {{ drupal_entity(entity_type, entity_id, view_mode, langcode, check_access) }}
