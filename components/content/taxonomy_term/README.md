# Taxonomy term

### Include

```html
{% include 'compass:taxonomy_term' %}
```

```html
{% include 'compass:taxonomy_term' ignore missing with {example_array_prop: [
  {example_one: "#", example_two: "First Example replacement item"},
  {example_one: "#", example_two: "Second Example replacement item"}
]} only %}
```
### Embed

```html
{% embed 'compass:taxonomy_term' %}
{% endembed %}
```

```html
{% embed 'compass:taxonomy_term' ignore missing with {example_array_prop: [
  {example_one: "#", example_two: "First Example replacement item"},
  {example_one: "#", example_two: "Second Example replacement item"}
]} only %}
  {% block content %}
    <div class="block-compass-taxonomy-term">
      <p class="block-compass-taxonomy-term__above">Example above message.</p>
      <div class="block-compass-taxonomy-term__wrapper">{{ parent() }}</div>
      <p class="block-compass-taxonomy-term__below">Example below message.</p>
    </div>
  {% endblock %}
{% endembed %}
```

### Render

```php
[
  '#type' => 'component',
  '#component' => 'compass:taxonomy_term',
  '#props' => [
    'example_array_prop' => [
      ['example_one' => "#", 'example_two' => "First Example replacement item"],
      ['example_one' => "#", 'example_two' => "Second Example replacement item"],
    ],
  ],
//   '#slots' => [
//     'content' => [
//       '#type' => 'html_tag',
//       '#tag' => 'p',
//       '#value' => 'This is the replacement of the component contents.',
//     ],
//   ],
];
```
