# Maintenance page

### Include

```html
{% include 'compass:maintenance_page' %}
```

```html
{% include 'compass:maintenance_page' ignore missing with {example_array_prop: [
  {example_one: "#", example_two: "First Example replacement item"},
  {example_one: "#", example_two: "Second Example replacement item"}
]} only %}
```
### Embed

```html
{% embed 'compass:maintenance_page' %}
{% endembed %}
```

```html
{% embed 'compass:maintenance_page' ignore missing with {example_array_prop: [
  {example_one: "#", example_two: "First Example replacement item"},
  {example_one: "#", example_two: "Second Example replacement item"}
]} only %}
  {% block content %}
    <div class="block-compass-maintenance-page">
      <p class="block-compass-maintenance-page__above">Example above message.</p>
      <div class="block-compass-maintenance-page__wrapper">{{ parent() }}</div>
      <p class="block-compass-maintenance-page__below">Example below message.</p>
    </div>
  {% endblock %}
{% endembed %}
```

### Render

```php
[
  '#type' => 'component',
  '#component' => 'compass:maintenance_page',
  '#props' => [
    'example_array_prop' => [
      ['example_one' => "#", 'example_two' => "First Example replacement item"],
      ['example_one' => "#", 'example_two' => "Second Example replacement item"],
    ],
  ],
//   '#slots' => [
//     'content' => [
//       '#type' => 'html_tag',
//       '#tag' => 'p',
//       '#value' => 'This is the replacement of the component contents.',
//     ],
//   ],
];
```
