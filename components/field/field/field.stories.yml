title: compass/components/field/Field
parameters:
  sourceLink: 'compass:field'
stories:
  - name: 1. Default
    args:
      entity_id: 1
      entity_type: node
      field_name: field_my_image
      view_mode: default
      langcode: ''
      check_access: true
      field_content: |
        {{ drupal_field(field_name, entity_type, entity_id, view_mode, langcode, check_access) }}
